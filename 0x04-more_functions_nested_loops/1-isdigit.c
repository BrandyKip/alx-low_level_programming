#include "main.h"

/*
 *  checks for a digit (0 through 9
 *  Return : (1) when true $ (0) otherwise
 */
int _isdigit(int c)
{
	if (c >= 34  && c <= 45)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
